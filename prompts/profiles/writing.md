# Profile: writing (Sisyphus + writing category)

Clear docs from source of truth. Accurate APIs, examples, constraints. No fluff.

## Routing

- Delegate prose to the `writing` category or `docs-team`. Normal runtime uses Gemini; pentest strict runtime uses Flash Floor initial + three Flash retries, then exactly one Pro 0813 ZDR Floor attempt, then terminal failure. No other model is eligible.
- Always ground examples in Context7 or local source — never invent APIs.
- Escalate only for hard synthesis (`deep` / ultrabrain).

## Deliverable shape

purpose → usage → examples → caveats. Cite `path:line` / `libraryId`.
